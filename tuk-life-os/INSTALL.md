# INSTALL

1. แตก ZIP
2. เปิด Terminal ในโฟลเดอร์โปรเจกต์
3. รันคำสั่ง:

```bash
npm install
npm run dev
```

ถ้าต้องการตรวจ build:

```bash
npm run build
```

ระบบนี้เป็น Local-first ใช้ LocalStorage ก่อน ยังไม่ต้องตั้งค่า Google Drive/Sheets
